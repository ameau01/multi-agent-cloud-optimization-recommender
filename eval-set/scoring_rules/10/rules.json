{
  "description": "Network latency to a payment provider cascades into compute application latency. Multi-tier reasoning REQUIRED: agent must recognize network is the cause, compute is the symptom, NOT scale compute. Network topology change with compute secondary. Single canonical category.",
  "finding_type_allowed": [
    "issue_found"
  ],
  "primary_tier_allowed": [
    "network"
  ],
  "secondary_tier_allowed": [
    "compute"
  ],
  "action_category_allowed": [
    "network_topology_change"
  ]
}

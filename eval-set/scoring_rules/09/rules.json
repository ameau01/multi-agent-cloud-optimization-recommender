{
  "description": "Bimodal peak/off-peak weekday pattern on multi-tier app. Agent should recommend scheduled scaling for off-peak cost reduction. Compute scaling policy change. scaling_policy_change is the canonical action; rightsizing was an alternative framing.",
  "finding_type_allowed": [
    "issue_found"
  ],
  "primary_tier_allowed": [
    "compute"
  ],
  "action_category_allowed": [
    "scaling_policy_change"
  ],
  "action_category_rationale": "Scheduled scaling is the canonical fix; static rightsizing is also defensible if the agent prefers a simpler intervention.",
  "secondary_tier_allowed": [
    "database"
  ]
}

{
  "description": "Compute is correctly sized; database is over-provisioned. Agent should rightsize ONLY the database (must not recommend compute changes). Database rightsizing. Single canonical category; pool_sizing was an alternative framing.",
  "finding_type_allowed": [
    "issue_found"
  ],
  "primary_tier_allowed": [
    "database"
  ],
  "action_category_allowed": [
    "rightsizing"
  ],
  "action_category_rationale": "Database rightsizing is the canonical fix; pool_sizing is defensible if framed as a connection-pool tuning.",
  "secondary_tier_allowed": [
    null
  ]
}

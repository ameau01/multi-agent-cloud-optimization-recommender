"""Project-level tests.

dataset-examples/tests/      validates the vendored scenario shape
eval-set/test_eval_set.py    validates the gold answers are well-formed
tests/ (this folder)         validates the orchestrator and evaluator
                             work end to end against mock recommendations
"""

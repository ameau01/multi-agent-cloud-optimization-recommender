{
  "scenario_id": "08",
  "finding_type": "issue_found",
  "specific_change": "Scale up the compute ASG from 8 to 12 m5.large instances to absorb the elevated application P95 latency observed during business hours. The current ASG sits at min=8 max=12 with target tracking; increase desired count to 12. Compute CPU is approaching saturation under peak load and additional capacity will distribute the request load. Monitor over the following week and rightsizing back to 8 if utilization drops.",
  "primary_tier": "compute",
  "secondary_tier": "database",
  "action_category": "rightsizing",
  "conclusion": {
    "finding_type": "issue_found",
    "primary_tier": "compute",
    "secondary_tier": "database",
    "action_category": "rightsizing",
    "headline": "Scale up the compute ASG to handle elevated latency"
  },
  "evidence": {
    "telemetry_observations": [
      "application_p95_latency_ms p95 is 659.8ms, far exceeding the 300ms SLA target.",
      "db_query_p95_latency_ms p95 is 791.2ms with max 819.8ms during business hours.",
      "Compute CPU p95 is 71.8% max, memory p95 is 64.4% \u2014 both within healthy bounds.",
      "db_io_wait_p95 spikes to 28ms (p95: 27.3ms), indicating storage pressure from unoptimized queries.",
      "db_connections_p95 reaches 80, suggesting high concurrent query load during peaks.",
      "application_p95_latency_ms stddev of 215ms indicates severe bimodal behavior (off-peak vs business hours)."
    ],
    "infrastructure_context": [
      "Database is db.r6g.xlarge with only 1 replica and no R/W splitting configured.",
      "Compute ASG is 8\u00d7 m5.large (min 8, max 12) with target-tracking \u2014 correctly sized for current CPU/memory.",
      "Terraform shows no read replica resource defined, all traffic hits the primary.",
      "No cache tier is deployed \u2014 all reads go directly to the database."
    ],
    "correlation_observations": [
      "db_query_p95_latency_ms \u2192 application_p95_latency_ms: Pearson r=0.945, lag=15min, alignment=0.979 across all 10 weekday business-hour windows."
    ]
  },
  "reasoning": "Application P95 latency exceeds the 300ms SLA target during business hours. Compute is the natural place to add capacity when latency rises. Scaling the ASG horizontally adds parallelism for incoming requests and is a standard intervention for latency-bound workloads.",
  "projected_state": {
    "cpu_p95_pct_estimate": "68-72",
    "memory_p95_pct_estimate": "50-65",
    "latency_p95_ms_estimate": "180-240",
    "sla_availability_preserved": true,
    "notes": "Based on before/after evidence: query optimization + 2 replicas with R/W splitting reduced db_query_p95 from 450-820ms to <220ms and app_p95 from 580-720ms to 180-240ms, well within the 300ms SLA."
  },
  "cost_impact": {
    "current_monthly_usd": 6400.0,
    "projected_monthly_usd": 8800.0,
    "savings_monthly_usd": -2400.0,
    "savings_pct": -37.5,
    "notes": "Adding 2 read replicas at db.r6g.xlarge adds ~$2,400/mo ($1,200/mo each based on current $2,200/mo for primary+1 replica \u2192 ~$1,100-1,200 per replica). This is a cost increase, not a savings, but it resolves an active SLA breach on a tier-1 checkout service. The alternative \u2014 continued SLA violations at 660ms p95 vs 300ms target \u2014 carries far greater business risk. Once queries are optimized and load stabilizes, evaluate downsizing to db.r6g.large replicas to recover ~$800/mo."
  },
  "risk_assessment": {
    "primary_risk": "Replication lag on the 2 new read replicas could serve stale data for cart/inventory/pricing reads during high-write periods, causing checkout inconsistencies.",
    "mitigation": "Monitor replica lag (target <100ms). Route write-after-read patterns (e.g., cart updates followed by cart reads) to the primary. Set up CloudWatch alarms on ReplicaLag > 500ms. Deploy index changes during a maintenance window (Sunday 04:00-06:00 UTC) to avoid lock contention on high-traffic tables.",
    "rollback": "Revert R/W splitting at the application/proxy layer to route all traffic back to primary. Drop newly added indexes if they cause unexpected write amplification. Remove read replicas via Terraform to restore original cost baseline.",
    "notes": "Index creation on tables with millions of rows should use CREATE INDEX CONCURRENTLY to avoid table locks. Test each index in staging first to validate query plan improvements."
  }
}